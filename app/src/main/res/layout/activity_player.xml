<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical"
                tools:activity=".activity.PlayerActivity">


    <include
        layout="@layout/giraffe_player"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>


    <LinearLayout
        android:id="@+id/layoutCustomActionBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#c0000000"
        android:gravity="right"
        android:orientation="vertical">

        <include layout="@layout/activity_player_titlebar"/>
    </LinearLayout>

    <LinearLayout
        android:id="@+id/layoutOutContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:gravity="right"
        android:orientation="vertical"
        android:padding="@dimen/space_16"
        android:visibility="visible">

        <Button
            android:id="@+id/buttonOut"
            android:layout_width="100dp"
            android:layout_height="wrap_content"
            android:background="@drawable/selector_button_rect_out"
            android:text="Out"
            android:textColor="#fff"
            android:textSize="@dimen/space_20"/>

        <Button
            android:id="@+id/buttonNotOut"
            android:layout_width="100dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_8"
            android:background="@drawable/selector_button_rect_out"
            android:text="Not Out"
            android:textColor="#fff"
            android:textSize="@dimen/space_20"/>
    </LinearLayout>
</RelativeLayout>
